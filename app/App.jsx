import React from 'react';
import styles from './App.scss';

export default class App extends React.Component {
  constructor(props) {
    super(props);
    // set initial state here as follows
    // this.state = {foo: 'bar'};
  }
  render() {
    return (
      <div className="app">
        HELLO
      </div>
    );
  }
}
